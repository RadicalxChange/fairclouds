---
// import directus from "../../../lib/directus";
// import { readItems } from "@directus/sdk";

// export async function getStaticPaths() {
//   // en pages
//   const pages = await directus.request(
//     readItems("wiki", {
//       fields: ["*", { translations: ["*"] }],
//     }),
//   );

//   // console.log(pages);

//   // to do: make translations work.

//   const paths = pages.map((page) => {
//     console.log(page.translations[0].slug);
//     const lang = "en";
//     const slug = page.translations[0].slug;
//     return { params: { lang: lang, slug: slug }, props: page };
//   });

//   // es pages

//   // combine together

//   return paths;
// }

// const { lang, slug } = Astro.params;
// const page = Astro.props;
---

<h1>WIP</h1>
